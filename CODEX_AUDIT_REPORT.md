# Codex Audit Report

This file is a short entrypoint for the current audit package.

The authoritative deep audit report is:

- `CODEX_DEEP_AUDIT/deep-audit-report.md`

The coverage ledger is:

- `CODEX_DEEP_AUDIT/repository_coverage_ledger.md`

Earlier notes about this checkout lacking Git metadata are obsolete. The local
repository metadata has been restored against
`https://github.com/r3e-network/neo-n4.git`, with `master` tracking
`origin/master`.
