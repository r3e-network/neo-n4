# P0/P1/P2 审计闭环账本 — 2026-07-14

本账本追踪最初针对公开
`master@688d87c37c0c12c84145f68f11bbe7d734ac574b` 提出的 8 个 P0、8 个 P1
和 4 个 P2 问题。它是验收账本，不是发布公告。包含本文件的源码版本仍是待审候选；
即使某行标记为“已实现”，在该版本完成审查、合并、部署和独立安全审计前，也不能描述为已发布。

## 状态术语

- **已实现并完成本地验证**：生产路径已 fail-closed，当前候选源码上的聚焦测试通过。
- **外部证据未完成**：源码修改无法替代公网部署、已充值交易、运维凭据或独立审查。

只要任何外部证据仍未完成，任何 Phase 都不能标记为生产完备。
[`testnet-evidence-status-2026-07-13.json`](../../audit/testnet-evidence-status-2026-07-13.json)
是旧公网部署已失效的机器可读真值来源。

## 当前验证快照

当前候选工作树已通过本地生产门禁：

- `dotnet build Neo.L2.sln` 为 0 warning、0 error；`dotnet format` 无改动；整个
  solution 通过 2,529 项测试，另有 4 项明确依赖部署或原生 fixture 的可选测试跳过；
- Release 覆盖率门禁达到 10,748 / 11,939 = 90.02%，全体已报告源码达到
  13,227 / 15,290 = 86.51%；覆盖率 runner 已加载并哈希固定所需的 RISC-V 原生 host；
- general rollup、gaming rollup、exchange Validium、privacy sidechain 四种 sample
  profile 均完成 3 个 batch，并通过运行后 audit；
- 28 个可部署/示例 Neo 合约均使用
  `external/neo-devpack-dotnet@82117c4799fde63e8c230e9e9696b66d794c6ed7`
  完成安全分析构建；verifier NEF SHA-256 为
  `71fcd4b8bbd83a894741cd143a4b2c41a6eea475de04d7d26f743b0d4858e221`；
- Rust format、全 workspace clippy（warning 视为 error）、release tests、RISC-V
  原生奇偶一致性和生产 C# 到原生 SP1 执行全部通过；
- 当前 guest 的 356-byte SP1 Groth16 向量同时通过 Rust host 与 NeoVM 验证；batch VK 为
  `007c49f6c46a00708a1529db5667bde2668bfa8f1ed269cdd87c2d32b5921a66`，
  guest ELF SHA-256 为
  `d512f85dda8df079a1abb1d75d35ac3bfd7a3fc54ecaa91f59fadcf74750c9a7`，
  public input 为
  `d2b9502bd5abc14c4abb48e8280e67a2a80347a3b72bcfff9c54c2f94991ade1`；
- 当前版本真实递归 release gate 在 1,118.00 秒内生成并由 host 验证 Gateway
  Groth16 proof；Gateway VK 为
  `0045e70b7add8250ad684cabc5aad40fe6f30d57d7df568504773df64849efa2`；
- TypeScript、Rust、Python 3.13、.NET SDK conformance 通过；TypeScript/Python
  package 构建通过；EVM 44 项、Solana 22 项、Experience Hub 10 项测试通过；
  3 项 live SDK 测试仍是部署 fixture 测试；
- CI helper 32 / 32 通过；`cargo-audit 0.22.2` 对 5 个生产 lockfile 均报告
  0 vulnerability，所有信息级警告均记录于
  [`rust-supply-chain-policy.md`](../rust-supply-chain-policy.md)。
- 实时执行 `dotnet list Neo.L2.sln package --vulnerable --include-transitive`
  解析了 106 个项目，并报告 0 个存在已知漏洞的 NuGet package。

## P0 协议与安全问题

| 问题 | 修复与权威代码 | 验收证据 | 状态 |
| --- | --- | --- | --- |
| P0-1 Validium 枚举比较 | `ChainRegistryContract.ValidateSecurityAndDa` 在注册时拒绝矛盾标签；`SettlementManagerContract.IsProofTypeCompatible` 使用显式兼容表，不再比较不同枚举的数字大小。 | `UT_ChainRegistry_Vm`、20 行组合矩阵、Validium + ZK + NeoFS 成功用例和原始比较源码扫描。 | **已实现并完成本地验证** |
| P0-2 跨 L2 重放 | `ExternalBridgeEscrowContract` 使用不可清除的非零目标 Neo chain domain；在 verifier 和 nonce 消耗之前验证签名消息域；部署工具强制提供该值。 | `UT_ExternalBridgeEscrow_Vm`、部署规划/实时部署测试以及两个 L2 共用 verifier 的重放测试。 | **已实现并完成本地验证** |
| P0-3 Gateway Root 未验证 | SettlementManager 和 MessageRouter 将 epoch、constituent root、global root、已终局批次引用、证明系统、VK 和 terminal proof 绑定；生产 profile 使用 `Sp1RecursiveRoundProver`，pass-through 不能满足生产配置。 | MessageRouter/SettlementManager VM、Gateway binding/publication、RPC publisher 和集成测试；当前版本真实递归 Gateway Groth16 生成和 host 验证 1 / 1 通过，耗时 1,118.00 秒。 | **已实现并完成本地验证** |
| P0-4 Fraud Proof 不可信 | Restricted proof v4 绑定 chain、batch、已提交 header、交易索引、pre-state proof、可执行 bytecode、post-state 和 replay domain；链上执行受限状态转换，验证成功后才能处罚。 | v4 编解码/奇偶一致性、Phase-3 集成以及 Restricted verifier/OptimisticChallenge VM 测试。 | **已实现并完成本地验证** |
| P0-5 执行与证明语义不一致 | `Sp1StatefulBatchExecutor`、状态见证源、原生执行器、`neo-execution-core` 和 SP1 guest 共用 `Sp1StatefulNeoVmV1`、canonical witness/effects/receipt/gas/public inputs。 | Stateful executor、witness source、原生/Rust golden vector、篡改测试、当前 terminal Groth16 向量和真实递归 proof 均使用相同已提交 witness 与 public-input 语义。 | **已实现并完成本地验证** |
| P0-6 缺少链上 ZK verifier | `NeoHub.Sp1Groth16Verifier` 实现锁定 SP1 6.2.1 SDK 所产出的 v6.1-compatible Groth16/BN254 wrapper pairing；router 绑定 proof system、精确 VK、public input 和 terminal verifier；生产部署拒绝 envelope-only。 | 源码/NEF/manifest 固定、费用上限、畸形点、所有绑定字段篡改，以及为当前 VK 重新生成的 356-byte 正向向量均在 Rust 与 NeoVM 通过。 | **已实现并完成本地验证** |
| P0-7 DA 等级虚标 | 本地持久化不再能伪装成 NeoFS/L1/DAC；真实 backend 生成并验证模式专用 receipt，生产配置缺少 backend 时 fail-closed。 | 105 项 DA 测试覆盖 REST 鉴权、对象定位、上传/读取/hash、L1 确认、DAC 阈值及本地模式拒绝；provider 真实远端演练仍需运维者执行。 | **已实现并完成本地验证；外部 provider 演练未完成** |
| P0-8 prover witness 为空 | Batch sealing 提交版本化 `ProofWitnessArtifactV1`；执行、审计、queue transport、SP1 host 和 settlement 消费相同字节；L1 message 与 block context 不再填零。 | Witness、state/native output serializer、SP1 prover/settlement stack、全栈集成，以及当前 terminal 与 recursive Groth16 proof。 | **已实现并完成本地验证** |

## P1 集成与发布问题

| 问题 | 修复与权威代码 | 验收证据 | 状态 |
| --- | --- | --- | --- |
| P1-1 RPC/SDK 未接通 | `L2RpcPlugin` 通过官方 `Neo.Plugins.RpcServer` 注册完整 N4 RPC；.NET、TypeScript、Rust、Python 共用一套 canonical conformance。 | HTTP RpcServer、SDK contract/error、四语言离线向量及必需 CI jobs；公网节点 live conformance 属于 P1-7。 | **已实现并完成本地验证** |
| P1-2 dBFT/签名/运维动作未接通 | Start 命令启动真实进程；operator broadcaster 通过 WIF 或外部 signer 协议签名、发送、对账并等待确认；dry-run 仍显式保留。 | Start/broadcaster/quick-path 测试、私网演练脚本和部署命令测试；有资金的当前版本执行属于 P1-7。 | **已实现并完成本地验证** |
| P1-3 消息与强制包含流程不完整 | 公共 consume 验证已终局 inclusion；强制包含事件持久扫描、排序入批并带证明确认；只有 finalized dBFT 证据能确定责任 sequencer 并进入治理处罚；生产拒绝零费用或缺少 bond。 | MessageRouter/ForcedInclusion VM、scanner/persistence、censorship、Settlement RPC 对账和集成测试。 | **已实现并完成本地验证** |
| P1-4 桥接未真正释放资产 | 验证后的 inbound 消息按 canonical asset/amount/recipient/domain/nonce/deadline 调用 payout adapter；L1/L2 payout outbox 持久且幂等。 | ExternalBridgeEscrow/L2PayoutAdapter VM、外部桥单元测试和 `UT_E2E_ExternalPayoutRelay`。 | **已实现并完成本地验证** |
| P1-5 治理密钥不可轮换 | Council rotation 与 proposal、epoch、阈值、timelock 和 replay protection 绑定，并覆盖 quorum-loss 恢复。 | GovernanceController VM 中的旧成员拒绝、非法阈值、重放、timelock、部分签名和 quorum-loss 测试。 | **已实现并完成本地验证** |
| P1-6 CI 与发布门禁缺失 | CI 覆盖 .NET、合约编译、Rust、真实 SP1、安全审计、四 SDK、EVM、Solana、experience hub 和 mdBook。terminal 与 recursive 真实 SP1 proof 是 required `sp1-host` job 的无条件步骤，不存在通过 workflow input 或 step-level skip 虚假变绿的路径。`master` 强制严格检查、1 个批准、最后一次 push 批准、旧批准失效、管理员约束、线性历史、会话解决、禁止 force-push/删除。 | CI 防回归测试与实时 GitHub branch-protection API；最终候选 push 后仍需全部远端检查重跑。 | **已实现；当前远端重跑待完成** |
| P1-7 测试网证据过期 | 旧部署已明确标为 superseded，不能用作当前 release 证据。 | JSON 证据显示所有安全关键合约均与当前源码不匹配且未部署 SP1 terminal verifier；本地和仓库 Actions 均无部署凭据。 | **外部证据未完成** |
| P1-8 队列不持久且可能丢批次 | Settlement、proof、Gateway、forced-inclusion 和 payout 使用持久、幂等状态机，包含对账、有限重试、poison 隔离、恢复、原子 readiness 发布和指标。SP1 settlement 先持久化并按字节验证不可变 proof artifact，再执行完整状态 CAS；重试/checkpoint 恢复修复崩溃窗口。私有 content-addressed prover queue 实施 16-GiB/64-task 背压，只在 durable `SettlementObserved` 后收到 hash-bound ack 才清理；daemon 删除可跨崩溃幂等恢复，禁止 TTL。聚合失败保留 constituents。 | 持久化、settlement、queue 权限/背压/ack-prune、Gateway outbox/publication、poison recovery、restart、重复接受、symlink/大小/hash/path 拒绝、并发 CAS 与运维恢复测试；build-support 测试还证明 verified ELF 被只读快照且拒绝 symlink 目标。 | **已实现并完成本地验证** |

闭环候选又经过一次内部只读复核，并修复四项生产加固 gap：required SP1 job 在 PR 上可跳过
真实 proof、proof 工件缺少 settlement-confirmed 清理生命周期、queue 权限/容量未强制，以及
build script 校验 ELF 后仍嵌入可变共享 target 路径。该结果属于内部复核证据，不能替代公开
生产上线前仍要求的独立外部审计。

## P2 文档与发布元数据问题

| 问题 | 修复 | 证据 | 状态 |
| --- | --- | --- | --- |
| P2-1 完成度虚标 | Phase 表分离设计、代码、集成、密码学强制、同版本部署和生产完备；没有 Phase 标记为 production-ready。 | README、docs/README、IMPLEMENTATION_STATUS、中英文对应页及 production-gap 测试。 | **已实现并完成本地验证** |
| P2-2 SDK/模块数量不一致 | 全仓统一为四套 typed SDK，并尽量以源树测试取代手写易漂移数量。 | sdk/README、TECH_STACK、conformance 文档和 production-gap 测试。 | **已实现并完成本地验证** |
| P2-3 项目归属冲突 | 包、合约、仓库与安全元数据统一为 R3E Network，同时保留“不受 Neo 组织背书”的明确声明。 | Directory.Build.props、生产合约 manifest、README、SECURITY 和 manifest 不变量测试。 | **已实现并完成本地验证** |
| P2-4 不存在的 release 声明 | 安全文档把签名 tag 改为未来发布策略，并明确 package version `0.1.0` 不等于 tag/release。 | SECURITY、中译页和 production-gap 测试。 | **已实现并完成本地验证** |

## 剩余发布顺序

1. 检查最终生成物和源码 diff，然后只提交并 push 到
   `https://github.com/r3e-network/neo-n4.git`。
2. 要求全部受保护 PR 检查和独立批准。
3. 使用有资金的运维凭据部署该精确审查版本，记录合约 hash、交易 ID 和完整
   deposit → execute → prove/finalize → withdraw、forced-inclusion、governance、recovery 演练。
4. 针对精确部署执行 NeoFS/L1/DAC provider 远端读取演练。
5. 在任何 production-ready 声明前取得独立安全审计。
