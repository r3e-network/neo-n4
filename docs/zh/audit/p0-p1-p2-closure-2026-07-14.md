# P0/P1/P2 审计闭环账本 — 2026-07-14

本账本追踪最初针对公开
`master@688d87c37c0c12c84145f68f11bbe7d734ac574b` 提出的 8 个 P0、8 个 P1
和 4 个 P2 问题。它是验收账本，不是发布公告。包含本文件的源码版本仍是待审候选；
即使某行标记为“已实现”，在该版本完成审查、合并、部署和独立安全审计前，也不能描述为已发布。

## 状态术语

- **已实现并完成本地验证**：生产路径已 fail-closed，当前候选源码上的聚焦测试通过。
- **外部证据未完成**：源码修改无法替代公网部署、已充值交易、运维凭据或独立审查。

只要任何外部证据仍未完成，任何 Phase 都不能标记为生产完备。
[`testnet-evidence-status-2026-07-13.json`](../../audit/testnet-evidence-status-2026-07-13.json)
是旧公网部署已失效的机器可读真值来源。

## 当前验证快照

当前候选工作树已通过本地生产门禁：

- `dotnet build Neo.L2.sln` 为 0 warning、0 error；`dotnet format` 无改动；38 个测试项目
  均生成唯一 TRX 证据并通过 2,587 项测试，另有 4 项明确依赖部署或原生 fixture 的可选测试跳过；
- Release 覆盖率门禁达到 11,120 / 12,351 = 90.03%，全体已报告源码达到
  13,704 / 15,838 = 86.53%；覆盖率 runner 已加载并哈希固定所需的 RISC-V 原生 host；
- general rollup、gaming rollup、exchange Validium、privacy sidechain 四种 sample
  profile 均完成 3 个 batch，并通过运行后 audit；
- 28 个可部署/示例 Neo 合约均使用
  `external/neo-devpack-dotnet@82117c4799fde63e8c230e9e9696b66d794c6ed7`
  完成安全分析构建；verifier NEF SHA-256 为
  `71fcd4b8bbd83a894741cd143a4b2c41a6eea475de04d7d26f743b0d4858e221`；
- Rust format、全 workspace clippy（warning 视为 error）、release tests、RISC-V
  原生奇偶一致性和生产 C# 到原生 SP1 执行全部通过；
- 本地生成的当前 guest 356-byte SP1 Groth16 向量同时通过 Rust host 与 NeoVM 验证；batch VK 为
  `007c49f6c46a00708a1529db5667bde2668bfa8f1ed269cdd87c2d32b5921a66`，
  guest ELF SHA-256 为
  `d512f85dda8df079a1abb1d75d35ac3bfd7a3fc54ecaa91f59fadcf74750c9a7`，
  public input 为
  `d2b9502bd5abc14c4abb48e8280e67a2a80347a3b72bcfff9c54c2f94991ade1`；
- 本地 ignored 递归 release-gate run 在 1,118.00 秒内生成并由 host 验证 Gateway
  Groth16 proof；Gateway VK 为
  `0045e70b7add8250ad684cabc5aad40fe6f30d57d7df568504773df64849efa2`。这些本地结果证明
  能力，但不能替代最终候选版本绑定的 protected CI 成功记录；
- TypeScript、Rust、Python 3.13、.NET SDK conformance 通过；TypeScript/Python
  package 构建通过；EVM 44 项、Solana 22 项、Experience Hub 10 项测试通过；
  3 项 live SDK 测试仍是部署 fixture 测试；
- CI helper 37 / 37 通过；`cargo-audit 0.22.2` 对 5 个生产 lockfile 均报告
  0 vulnerability，所有信息级警告均记录于
  [`rust-supply-chain-policy.md`](../rust-supply-chain-policy.md)。
- 实时执行 `dotnet list Neo.L2.sln package --vulnerable --include-transitive`
  解析了 106 个项目，并报告 0 个存在已知漏洞的 NuGet package。

## P0 协议与安全问题

| 问题 | 修复与权威代码 | 验收证据 | 状态 |
| --- | --- | --- | --- |
| P0-1 Validium 枚举比较 | `ChainRegistryContract.ValidateSecurityAndDa` 在注册时拒绝矛盾标签；`SettlementManagerContract.IsProofTypeCompatible` 使用显式兼容表，不再比较不同枚举的数字大小。 | `UT_ChainRegistry_Vm`、20 行组合矩阵、Validium + ZK + NeoFS 成功用例和原始比较源码扫描。 | **已实现并完成本地验证** |
| P0-2 跨 L2 重放 | `ExternalBridgeEscrowContract` 使用不可清除且显式的目标 Neo chain domain；零表示 Neo L1，L2 domain 非零。在 verifier 和 nonce 消耗之前验证签名消息域；部署工具强制提供显式值。 | `UT_ExternalBridgeEscrow_Vm`、部署规划/实时部署测试以及两个 L2 共用 verifier 的重放测试。 | **已实现并完成本地验证** |
| P0-3 Gateway Root 未验证 | SettlementManager 和 MessageRouter 将 epoch、constituent root、global root、已终局批次引用、证明系统、VK 和 terminal proof 绑定；生产 profile 使用 `Sp1RecursiveRoundProver`，pass-through 不能满足生产配置。 | MessageRouter/SettlementManager VM、Gateway binding/publication、RPC publisher 和集成测试；本地真实递归 Gateway proof 已通过，最终精确候选仍需 protected CI proof run。 | **已实现并完成本地验证；版本绑定 CI proof 待完成** |
| P0-4 Fraud Proof 不可信 | Restricted proof v4 绑定 chain、batch、已提交 header、交易索引、pre-state proof、可执行 bytecode、post-state 和 replay domain；链上执行受限状态转换，验证成功后才能处罚。 | v4 编解码/奇偶一致性、Phase-3 集成以及 Restricted verifier/OptimisticChallenge VM 测试。 | **已实现并完成本地验证** |
| P0-5 执行与证明语义不一致 | `Sp1StatefulBatchExecutor`、状态见证源、原生执行器、`neo-execution-core` 和 SP1 guest 共用 `Sp1StatefulNeoVmV1`、canonical witness/effects/receipt/gas/public inputs。ChainRegistry 原子注册 production profile 携带的同一个不可变非零 genesis root，SettlementManager 在执行或终局 batch 1 前绑定该根。 | Stateful executor、witness source、执行器重建后的重启恢复、canonical settlement 连续性、ChainRegistry/SettlementManager VM 信任锚测试、原生/Rust golden vector、篡改测试、当前 terminal Groth16 向量和真实递归 proof 均使用相同已提交 witness 与 public-input 语义。 | **已实现并完成本地验证** |
| P0-6 缺少链上 ZK verifier | `NeoHub.Sp1Groth16Verifier` 实现锁定 SP1 6.2.1 SDK 所产出的 v6.1-compatible Groth16/BN254 wrapper pairing；router 绑定 proof system、精确 VK、public input 和 terminal verifier；生产部署拒绝 envelope-only。 | 源码/NEF/manifest 固定、费用上限、畸形点、所有绑定字段篡改，以及为当前 VK 重新生成的 356-byte 正向向量均在 Rust 与 NeoVM 通过。 | **已实现并完成本地验证** |
| P0-7 DA 等级虚标 | 本地持久化不再能伪装成 NeoFS/L1/DAC；真实 backend 生成并验证模式专用 receipt，生产配置缺少 backend 时 fail-closed。 | 105 项 DA 测试覆盖 REST 鉴权、对象定位、上传/读取/hash、L1 确认、DAC 阈值及本地模式拒绝；provider 真实远端演练仍需运维者执行。 | **已实现并完成本地验证；外部 provider 演练未完成** |
| P0-8 prover witness 为空 | Batch sealing 提交版本化 `ProofWitnessArtifactV1`；执行、审计、queue transport、SP1 host 和 settlement 消费相同字节；L1 message 与 block context 不再填零。 | Witness、state/native output serializer、SP1 prover/settlement stack、全栈集成，以及本地 terminal/recursive Groth16 proof；最终候选仍需版本绑定 protected CI 证据。 | **已实现并完成本地验证；版本绑定 CI proof 待完成** |

## P1 集成与发布问题

| 问题 | 修复与权威代码 | 验收证据 | 状态 |
| --- | --- | --- | --- |
| P1-1 RPC/SDK 未接通 | `L2RpcPlugin` 通过官方 `Neo.Plugins.RpcServer` 注册完整 N4 RPC；.NET、TypeScript、Rust、Python 共用一套 canonical conformance。 | HTTP RpcServer、SDK contract/error 和四语言离线向量；四 SDK 对同一精确部署 N3/N4 fixture 的 live conformance 仍是发布门禁。 | **离线集成已验证；live 精确部署 conformance 未完成** |
| P1-2 dBFT/签名/运维动作未接通 | Start 命令启动真实进程；operator broadcaster 通过 WIF 或外部 signer 协议签名、发送、对账并等待确认。私网 harness 要求经审阅的配置/二进制，并执行显式 dry-run launch preflight，不再无配置阻塞启动。runtime/plugin 二进制仅白名单暂存到当前用户专属 OS 临时目录；钱包、节点数据、任意 JSON、隐藏路径与链接不复制，`finally` 无法清理临时树时运行失败。sequencer preflight 会打开外部 NEP-6 钱包，实际解密配置委员会账户，并验证私钥派生公钥精确等于对应 validator；不支持的 HSM adapter 文件在完成单独审阅的 Neo.CLI wallet-factory 集成前 fail closed。 | Start 测试包含多账户钱包中的委员会密文替换；broadcaster/quick-path 测试、可执行 PowerShell 暂存 fixture、源码/AST 门禁和部署命令测试覆盖其余边界；有资金的并发进程/dBFT 执行仍是精确部署发布证据。 | **实现与 preflight 已验证；有资金进程演练未完成** |
| P1-3 消息与强制包含流程不完整 | 公共 consume 验证已终局 inclusion，并在只读 root 查询前预写重放标记，失败 root/proof 原子回滚；强制包含事件持久扫描、排序入批并带证明确认；生产固定 Neo N3 原生 GAS，拒绝替代 NEP-17、零费用或缺少 bond。enqueue 向经过 witness 的 transaction sender 收费，而不是误用入口 invocation script hash。权限开放的逾期上报只能使用零地址归因，调用者不能构陷 bonded sequencer；只有 governance 在独立复核已终局 dBFT 证据后才能指定 slash 目标。`ICensorshipAttributionProvider` 是可选证据适配器，而不是自动处罚授权。 | MessageRouter/ForcedInclusion VM（包括真实 native-GAS sender 余额转移、拒绝非原生 token、失败 proof 回滚、零地址事件、拒绝调用者自带归因及有效报告后非 owner 罚没）、scanner/persistence、censorship、Settlement RPC 对账和集成测试。 | **已实现并完成本地验证；有资金多进程治理演练未完成** |
| P1-4 桥接未真正释放资产 | 验证后的 inbound 消息按 canonical asset/amount/recipient/domain/nonce/deadline 调用 payout adapter；L1/L2 payout outbox 持久且幂等。 | ExternalBridgeEscrow/L2PayoutAdapter VM、外部桥单元测试和 `UT_E2E_ExternalPayoutRelay`。 | **已实现并完成本地验证** |
| P1-5 治理密钥不可轮换 | Council rotation 与 proposal、epoch、阈值、timelock 和 replay protection 绑定，并覆盖 quorum-loss 恢复。生产部署把 SettlementManager 与 ChainRegistry 接到该 controller 后执行不可逆 governance lock：hot owner 不能再替换安全依赖、ChainRegistry proposal authority 或直接回滚批次；异常的已终局头部回滚必须匹配绑定 executing contract、精确、已批准、已过 timelock 且只能消费一次的 proposal payload，防止跨 SettlementManager 部署重放。live deploy 必须显式提供互异的 M-of-N council，M >= 2，并拒绝隐式 1-of-1 governance。 | GovernanceController、SettlementManager、ChainRegistry VM、DeployPlanner 与 LiveDeployCommand 测试覆盖旧成员拒绝、非法阈值、重放、timelock、部分签名、quorum-loss 恢复、owner 锁定、锁后 controller 不可变、executing-contract 与精确 proposal 绑定、proposal 重放拒绝和部署 read-back。 | **已实现并完成本地验证** |
| P1-6 CI 与发布门禁缺失 | CI 覆盖 .NET、合约编译、Rust、安全审计、四 SDK、EVM、Solana、experience hub 和 mdBook。普通 PR/`master` 运行要求快速的 `SP1 compatibility and manual release proof gate`，且必须把昂贵真实证明矩阵保持为 **skipped**。terminal 与 recursive 真实 SP1 proof 仅在手动 `workflow_dispatch` 触发的三条 `sp1-release-gates` lane 上运行；汇总门禁随后要求每条 lane 成功，并禁止 mock/dummy。`master` 强制严格检查、1 个批准、最后一次 push 批准、旧批准失效、管理员约束、线性历史、会话解决、禁止 force-push/删除。 | CI 防回归测试与实时 GitHub branch-protection API；最终候选仍需成功的真实证明 release dispatch 与独立批准。 | **已实现；最终候选检查和批准待完成** |
| P1-7 测试网证据过期 | 旧部署已明确标为 superseded，不能用作当前 release 证据。 | JSON 证据显示所有安全关键合约均与当前源码不匹配且未部署 SP1 terminal verifier。`artifacts/` 下被忽略的本地 operator wallet 材料未提交，也不是最终候选的有资金部署证据，禁止据此宣称完成；最终部署仍需单独受控凭据和审阅后的发布记录。 | **外部证据未完成** |
| P1-8 队列不持久且可能丢批次 | Settlement、proof、Gateway、forced-inclusion 和 payout 使用持久、幂等状态机，包含对账、有限重试、poison 隔离、恢复、原子 readiness 发布和指标。SP1 settlement 在执行/DA 前拒绝 batch 0、batch-1 root 与不可变链上 genesis trust anchor 不同、缺失前序、前序 block overflow 及 block/state 断链，再持久化并按字节验证不可变 proof artifact 后执行完整状态 CAS；`WireProduction` 拒绝易失 witness/forced-event store。`Pending`、`Challengeable` 只表示已观察而非最终化。`Reverted` 尾部会与 proof 一起原子隔离，恢复经过认证的状态快照并幂等完成回滚检查点，之后才允许同编号重新提交。启动恢复会逐个查询本地 artifact 的 L1 状态，并在任何恢复副作用前验证本地 proof manifest、连续最终性和 canonical root。私有 content-addressed prover queue 实施 16-GiB/64-task 背压，只在 durable `SettlementFinalized` 后收到 hash-bound ack 才清理；daemon 删除可跨崩溃幂等恢复，禁止 TTL。聚合失败保留 constituents。 | 持久化、ChainRegistry/SettlementManager VM genesis 绑定、执行前连续性所有零副作用边界、生产耐久能力、settlement finality/rollback/restart、queue 权限/背压/ack-prune、Gateway outbox/publication、poison recovery、执行器重建后的 restart、重复接受、symlink/大小/hash/path 拒绝、快照与 CAS 竞态以及运维恢复测试；build-support 测试还证明 verified ELF 被只读快照且拒绝 symlink 目标。 | **已实现并完成本地验证** |

闭环候选又经过一次内部只读复核，并修复八项生产加固 gap：required SP1 job 在 PR 上可跳过
真实 proof、proof 工件缺少 settlement-confirmed 清理生命周期、queue 权限/容量未强制，以及
build script 校验 ELF 后仍嵌入可变共享 target 路径，SettlementManager hot owner 可绕过
timelock governance 重接依赖或直接回滚最新已终局头部，ChainRegistry 会把未定义 admission
mode 当成 permissionless，governance lock 后仍能替换 ChainRegistry controller，以及生产部署
只配置 ChainRegistry governance 却没有执行并回读验证不可逆 lock。该结果
属于内部复核证据，不能替代公开生产上线前仍要求的独立外部复核。

## P2 文档与发布元数据问题

| 问题 | 修复 | 证据 | 状态 |
| --- | --- | --- | --- |
| P2-1 完成度虚标 | Phase 表分离设计、代码、集成、密码学强制、同版本部署和生产完备；没有 Phase 标记为 production-ready。 | README、docs/README、IMPLEMENTATION_STATUS、中英文对应页及 production-gap 测试。 | **已实现并完成本地验证** |
| P2-2 SDK/模块数量不一致 | 全仓统一为四套 typed SDK，并尽量以源树测试取代手写易漂移数量。 | sdk/README、TECH_STACK、conformance 文档和 production-gap 测试。 | **已实现并完成本地验证** |
| P2-3 项目归属冲突 | 包、合约、仓库与安全元数据统一为 R3E Network，同时保留“不受 Neo 组织背书”的明确声明。 | Directory.Build.props、生产合约 manifest、README、SECURITY 和 manifest 不变量测试。 | **已实现并完成本地验证** |
| P2-4 不存在的 release 声明 | 安全文档把签名 tag 改为未来发布策略，并明确 package version `0.1.0` 不等于 tag/release。 | SECURITY、中译页和 production-gap 测试。 | **已实现并完成本地验证** |

## 剩余发布顺序

1. 检查最终生成物和源码 diff，然后只提交并 push 到
   `https://github.com/r3e-network/neo-n4.git`。
2. 要求全部受保护 PR 检查和独立批准。
3. 使用有资金的运维凭据部署该精确审查版本，记录合约 hash、交易 ID 和完整
   deposit → execute → prove/finalize → withdraw、forced-inclusion、governance、recovery 演练。
4. 针对精确部署执行 NeoFS/L1/DAC provider 远端读取演练。
5. 在任何 production-ready 声明前取得独立安全审计。
